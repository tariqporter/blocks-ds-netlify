import React from "react"

const { BackIcon,
        DownIcon,
        NextIcon,
        UpIcon } = require('blocks-react').Icons;

const IconArrowExamples = () => (
  <div>
    <BackIcon />
    <DownIcon />
    <NextIcon />
    <UpIcon />
  </div>
);

export default IconArrowExamples;
